# TODO: bonus — express elevator(s) that only stop at a subset of floors (e.g. every
# 5th floor, or a sky-lobby split), forcing transfers for requests between non-served
# floors. Pairs with core/models.py needing an elevator "serviceable floors" concept.
