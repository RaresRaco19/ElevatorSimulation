# TODO: tests for src/elevator_sim/core/engine.py
# - ticks advance one floor at a time
# - requests are never consumed ahead of their `time`
# - capacity is never exceeded
# - direction logic (e.g. don't reverse mid-run of same-direction stops) is respected
# - simulation terminates once all known requests are served
