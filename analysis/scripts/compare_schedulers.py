# TODO: read multiple runs (one per scheduler, same input scenario) from an outputs
# directory and plot a fairness-vs-efficiency comparison (e.g. avg/max wait time per
# scheduler) to analysis/figures/scheduler_comparison.png.
