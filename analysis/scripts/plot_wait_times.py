# TODO: read a single run's passenger_stats.json (+ per-passenger detail, if we end up
# logging that rather than just aggregates) and plot wait_time / total_time distributions
# (e.g. histogram or box plot) to analysis/figures/<run>_wait_times.png.
